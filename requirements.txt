# Dépendances communes à tous les services FoodExpress (squelette FastAPI)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic-settings>=2.2
sqlalchemy>=2.0
psycopg[binary]>=3.1   # driver PostgreSQL (SQLAlchemy 2)
pika>=1.3              # client RabbitMQ
httpx>=0.27            # appels REST sortants + résilience
